VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Garmin Connect login for scripts/garmin_sync.py (server-side only — NOT
# VITE_-prefixed, so these are never bundled into the frontend). On first run
# you may be prompted for an MFA code; the session token is then cached.
GARMIN_EMAIL=
GARMIN_PASSWORD=
